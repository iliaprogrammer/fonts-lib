# fontlib

A simple Python tool for converting English text into different Unicode font styles.

## Example
usage : enfont(txt : str, font : dict)
print(enfont("hello", f1))
you can use 39 fonts from f1 to f39
